AGENT_REPORT

Дата: 2026-04-29
Проект: `G:\AI\_MY_PROGRAMMING_3\VOICE-OVE-RECORDING-QWEEN3\`

Текущее состояние

`TASK-002` выполнена.
`TASK-003` выполнена.
`TASK-004` выполнена.

Рабочая схема окружения теперь такая:
- `venv`: `C:\PythonEnvs\qwen3-tts\`
- исполняемые файлы (`python.exe`, `pip.exe`) запускаются из `C:\PythonEnvs\qwen3-tts\Scripts\`
- `site-packages` вынесен на `G:` через junction:
  - `C:\PythonEnvs\qwen3-tts\Lib\site-packages`
  - `=> G:\AI\_MY_PROGRAMMING_3\VOICE-OVE-RECORDING-QWEEN3\_venv-site-packages`

Почему так сделано

Изначально архитектурное решение было таким:
- не держать `venv` на `G:`, потому что Windows блокирует запуск бинарников из
  `venv\Scripts\` на этом диске
- перенести `venv` на `C:`

Это сработало только частично:
- `venv` на `C:` создавался нормально
- но при установке `torch` на `C:` не хватило места

Фактическое наблюдение:
- после частичной установки на `C:` свободного места оставалось около `57 KB`
- `torch` падал на `WinError 126` при загрузке DLL

Поэтому был выбран компромисс:
- оставить исполняемую часть `venv` на `C:`
- вынести тяжёлый `site-packages` на `G:`
- связать их junction-ом

Это сохраняет главный принцип:
- запуск идёт из `C:`
- тяжёлые пакеты не упираются в лимит пространства на `C:`

Что было сделано по TASK-002

1. Удалён сломанный `venv` внутри проекта на `G:`
   - удаление потребовало `takeown`, потому что часть файлов была недоступна

2. Создан новый `venv`:
   - `C:\PythonEnvs\qwen3-tts`

3. Пересобрана схема `site-packages`:
   - удалён локальный `site-packages` внутри `venv` на `C:`
   - создан junction на:
     - `G:\AI\_MY_PROGRAMMING_3\VOICE-OVE-RECORDING-QWEEN3\_venv-site-packages`

4. Восстановлен `pip` внутри нового `venv`
   - через `python -m ensurepip --upgrade`

5. Установлен PyTorch CUDA stack:
   - `torch 2.6.0+cu124`
   - `torchvision 0.21.0+cu124`
   - `torchaudio 2.6.0+cu124`

6. Установлен `Qwen3-TTS` из уже клонированного репозитория:
   - editable install из:
     - `G:\AI\_MY_PROGRAMMING_3\VOICE-OVE-RECORDING-QWEEN3\Qwen3-TTS`

7. Выполнена проверка:
   - `torch.__version__` -> `2.6.0+cu124`
   - `torch.cuda.is_available()` -> `True`

Что было сделано по TASK-003

Создан shared tool:
- `G:\AI\_MY_PROGRAMMING_3\_AGENT-TOOLS\bg-runner\launch.py`
- `G:\AI\_MY_PROGRAMMING_3\_AGENT-TOOLS\bg-runner\status.py`
- `G:\AI\_MY_PROGRAMMING_3\_AGENT-TOOLS\bg-runner\README.md`

Реализованное поведение:
- `launch.py`
  - создаёт каталог логов
  - генерирует `job_id`
  - запускает команду в фоне
  - сразу пишет JSON status file
  - пишет stdout/stderr в `.log`
  - печатает `job_id`, `pid`, `log_path`

- `status.py`
  - читает JSON status file
  - проверяет, жив ли PID
  - если процесс исчез, обновляет статус на `done` или `error`
  - смотрит `Traceback` в логе для различения `done/error`
  - печатает статус, pid, timestamps и хвост лога

Smoke test

Запускался:
- системным `C:\Users\Yuri\AppData\Local\Programs\Python\Python313\python.exe`
- командой `ping 127.0.0.1 -n 5`

Результаты:
- первый smoke test подтвердил, что фоновой запуск и JSON-статус работают
- после этого был внесён маленький фикс в `launch.py`, потому что лог оставался пустым
- затем был внесён маленький фикс в `status.py`, потому что печать хвоста лога падала
  на `UnicodeEncodeError` в консоли Windows

Финальный успешный smoke test:
- `job_id`: `smoke-test-2_20260429_151051`
- статус: `done`
- `started_at`: `2026-04-29T15:10:51`
- `finished_at`: `2026-04-29T15:11:40`
- лог прочитан успешно

Замечание по выводу лога:
- строки `ping` в консоли отображаются с заменами символов (`???`), потому что
  текст идёт из Windows console/OEM-кодировки
- это не ломает сам `bg-runner`
- статус, лог-файл и хвост лога при этом работают

Что было сделано по TASK-004

Цель:
- выполнить первый русский синтез через Qwen3-TTS

Фактическая модель:
- `Qwen/Qwen3-TTS-12Hz-1.7B-Base`

Почему не `0.6B`:
- в актуальном `AGENT_CONTEXT.md` зафиксировано, что нужной `0.6B` модели нет
- для первого теста используется `1.7B-Base`

Подготовка:
- создан `test_ru.py` в корне проекта
- Hugging Face cache перенаправлен на:
  - `G:\hf-cache`
  - `G:\hf-cache\hub`

Первая попытка:
- `job_id`: `qwen3-tts-test_20260429_153017`
- процесс завис на ранней стадии загрузки модели
- в логе было сообщение:
  - `Xet Storage is enabled ... but hf_xet is not installed`
- `.incomplete` blob оставался нулевого размера
- этот запуск был остановлен

Исправление:
- установлен пакет `hf_xet` в `qwen3-tts` venv

Успешный запуск:
- `job_id`: `qwen3-tts-test-2_20260429_153355`
- запуск шёл через shared `bg-runner`
- использовался:
  - `C:\PythonEnvs\qwen3-tts\Scripts\python.exe`

Результат по логу:
- модельные файлы были скачаны в `G:\hf-cache`
- `Model ready in 61.38s`
- `Generation done in 14.73s`
- сохранён файл:
  - `G:\AI\_MY_PROGRAMMING_3\VOICE-OVE-RECORDING-QWEEN3\output\test_ru_1.7b.wav`
- вывод:
  - `24000 Hz`
  - `140160 samples`

Финальный статус job:
- `status=done`
- `started_at=2026-04-29T15:33:55`
- `finished_at=2026-04-29T15:35:37`

Нефатальные замечания:
- `SoX could not be found!`
  - это предупреждение
  - синтез при этом завершился успешно
- `flash-attn is not installed`
  - это предупреждение
  - запуск шёл на обычной PyTorch attention path и отработал

Что теперь считается рабочим

Правильный способ запуска для проекта:

```powershell
C:\PythonEnvs\qwen3-tts\Scripts\python.exe <script>
```

Правильный способ установки пакетов:

```powershell
C:\PythonEnvs\qwen3-tts\Scripts\pip.exe install ...
```

Для Hugging Face моделей:

```powershell
$env:HF_HOME = "G:\hf-cache"
$env:HF_HUB_CACHE = "G:\hf-cache\hub"
```

Если снова будут большие скачивания:
- задавать `TMP`, `TEMP`, `PIP_CACHE_DIR` в `G:\tmp`

Текущее итоговое состояние

Проект now has:
- рабочее Qwen3-TTS окружение
- рабочий shared `bg-runner`
- первый успешный русский WAV-результат

Следующий логический шаг

Новых открытых задач в текущем `AGENT_CONTEXT.md` больше нет.

Проблема для архитектора: ложное завершение bg-runner на повторном TTS-запуске

Дата наблюдения:
- `2026-04-29`

Сценарий:
- после успешного первого синтеза был запущен ещё один тест через shared `bg-runner`
- команда запуска:
  - `C:\PythonEnvs\qwen3-tts\Scripts\python.exe test_ru.py`
- затем для более прозрачного лога:
  - `C:\PythonEnvs\qwen3-tts\Scripts\python.exe -u test_ru.py`

Какие job это показали:
- `qwen3-tts-test-3_20260429_161325`
- `qwen3-tts-test-4_20260429_161416`

Что наблюдается:
- `status.py` почти сразу показывает `status=done`
- в JSON фиксируется `finished_at`, как будто job завершена успешно
- при этом новый `output\test_ru_1.7b.wav` не обновляется
- лог на момент ложного `done` содержит только раннюю стадию:
  - `Loading model: Qwen/Qwen3-TTS-12Hz-1.7B-Base`
- после отдельной проверки процессов выяснилось, что реальные `python.exe test_ru.py` всё ещё живы
- по повторной проверке лога видно, что процесс на самом деле продолжает работу и доходит до:
  - `Model ready in 24.89s`
  - `Generating Russian synthesis...`

Подтверждение проблемы:
- `bg-runner` считает задачу завершённой, хотя дочерние Python-процессы продолжают жить
- в системе одновременно висели несколько процессов:
  - `python.exe test_ru.py`
  - `python.exe -u test_ru.py`
- то есть проблема сейчас не в самом запуске Qwen3-TTS, а в механике отслеживания процесса внутри `bg-runner`

Рабочая гипотеза:
- `launch.py` сохраняет PID промежуточного процесса, который завершается раньше
- реальная тяжёлая задача продолжает жить как другой процесс
- из-за этого `status.py` проверяет не тот PID и слишком рано ставит `done`

Почему это важно:
- текущий `done` в `bg-runner` нельзя считать достоверным признаком успешного завершения TTS-задачи
- для длинных inference jobs это ломает основную идею инструмента: надёжно сообщать, идёт задача или уже действительно закончилась

Что нужно проверить/решить:
- какой именно PID сейчас сохраняет `launch.py`
- не запускается ли команда через промежуточный shell-процесс, который быстро умирает
- нужно ли отслеживать дерево процессов, а не один PID
- нужен ли другой способ запуска, чтобы `bg-runner` держал именно реальный рабочий процесс, а не обёртку

Текущее практическое состояние:
- повторный TTS-тест был действительно запущен
- `bg-runner` в текущем виде ненадёжно репортит состояние для такого Python workload
- перед следующими чистыми тестами желательно остановить старые висящие `test_ru.py`, чтобы не копить параллельные генерации и не путать диагностику

Исправление после анализа

Что было изменено в shared `bg-runner`:
- в `launch.py`
  - убран `shell=True`
  - команда теперь запускается как список аргументов через `shlex.split(..., posix=False)`
  - `shell=False`
- в `status.py`
  - для Windows убрана проверка живости процесса через `os.kill(pid, 0)`
  - вместо этого используется `tasklist` по конкретному PID

Почему понадобился второй фикс:
- после правки `launch.py` проблема раннего `done` осталась даже на `ping 127.0.0.1 -n 20`
- это показало, что запуск был уже ближе к правильному, но `status.py` всё ещё неверно определял живость PID на Windows

Проверка после исправления:
- smoke test:
  - `job_id`: `smoke-test-5_20260429_162319`
- промежуточная проверка:
  - `status=running`
  - `finished_at=None`
- финальная проверка после завершения:
  - `status=done`
  - `finished_at=2026-04-29T16:23:53`

Новый длинный TTS-запуск после фикса:
- `job_id`: `qwen3-long-test_20260429_162414`
- статус на стартовой проверке:
  - `running`
- по логу дошёл до:
  - `Model ready in 15.26s`
  - `Generating Russian synthesis...`

Вывод:
- проблема ложного раннего `done` для фоновых задач на Windows исправлена
- shared `bg-runner` теперь корректно показывает `running` для реального длительного процесса

TASK-005 result

Goal:
- synthesize Russian text from file with Eugene voice reference

Successful job:
- `job_id`: `qwen3-eugene-test_20260429_164629`
- `started_at`: `2026-04-29T16:46:29`
- `finished_at`: `2026-04-29T16:51:23`
- final status: `done`

Script/runtime:
- script:
  - `G:\AI\_MY_PROGRAMMING_3\VOICE-OVE-RECORDING-QWEEN3\test_ru.py`
- reference audio:
  - `G:\AI\_MY_PROGRAMMING_3\VOICE-OVE-RECORDING\voices\eugene.wav`
- text source:
  - `G:\AI\_MY_PROGRAMMING_3\VOICE-OVE-RECORDING\text\_za_lopatoj_4.txt`

Timings from log:
- `Model ready in 9.13s`
- `Generation done in 79.49s`

Output:
- file:
  - `G:\AI\_MY_PROGRAMMING_3\VOICE-OVE-RECORDING-QWEEN3\output\test_ru_1.7b_eugene.wav`
- metadata from log:
  - `24000 Hz`
  - `971520 samples`
- file size on disk:
  - `1,943,084 bytes`
- `LastWriteTime`:
  - `2026-04-29 16:48:04`

Warnings observed but non-fatal:
- `SoX could not be found!`
- `flash-attn is not installed`

TASK-006 result

Goal:
- make bg-runner finalize `.json` status automatically after the main process exits

Changes made:
- created:
  - `G:\AI\_MY_PROGRAMMING_3\_AGENT-TOOLS\bg-runner\_finalizer.py`
- updated:
  - `G:\AI\_MY_PROGRAMMING_3\_AGENT-TOOLS\bg-runner\launch.py`

Implementation:
- `launch.py` now starts a lightweight companion finalizer right after launching the main job
- `_finalizer.py` waits for the main PID and then updates:
  - `status`
  - `finished_at`
  - `error` if `Traceback` is found in the log

Important implementation note:
- the first polling version of `_finalizer.py` still got stuck on Windows
- it was replaced with a more reliable Windows wait path:
  - `OpenProcess(SYNCHRONIZE)` + `WaitForSingleObject`
- fallback polling remains for non-Windows environments

Smoke test:
- job:
  - `job_id=smoke-test-7_20260429_171215`
- command:
  - `ping 127.0.0.1 -n 8`

Verification method:
- checked the JSON file directly
- did not call `status.py`

Observed result:
- JSON updated automatically to:
  - `status=done`
  - `finished_at=2026-04-29T17:12:22`

Conclusion:
- TASK-006 is working
- bg-runner no longer requires a manual `status.py` call to finalize completed jobs
